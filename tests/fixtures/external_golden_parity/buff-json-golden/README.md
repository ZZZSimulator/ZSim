# Buff Timeline JSON External Golden Fixture

This fixture backs `US-003` external golden buff timeline parity tests. It is a
compact result directory with cached `buff_log/buff_timeline_data.json` input
only, preserving the public `Task`, `Start`, `Finish`, and `Value` fields.
